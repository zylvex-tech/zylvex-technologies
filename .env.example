# =============================================================================
# ZYLVEX TECHNOLOGIES — Environment Variables Reference
# Copy relevant sections to each service's .env file
# Generate secrets with: python -c "import secrets; print(secrets.token_hex(32))"
# =============================================================================

# --- Shared Auth Service (shared/auth/.env) ---
DATABASE_URL=postgresql://auth_user:auth_pass@localhost:5432/auth_db
JWT_SECRET=your-generated-secret-here
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=30
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081
# SendGrid — leave empty to log emails to console instead of sending (dev mode)
SENDGRID_API_KEY=
EMAIL_FROM=noreply@zylvex.io
EMAIL_FROM_NAME=Zylvex
# Frontend URL — used to build email verification and password reset links
FRONTEND_URL=http://localhost:3000

# --- Spatial Canvas Backend (spatial-canvas/backend/.env) ---
DATABASE_URL=postgresql://spatial_user:spatial_pass@localhost:5433/spatial_db
AUTH_SERVICE_URL=http://localhost:8001
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8081

# --- Mind Mapper Backend (mind-mapper/backend-services/.env) ---
DATABASE_URL=postgresql://mm_user:mm_pass@localhost:5434/mm_db
AUTH_SERVICE_URL=http://localhost:8001
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:19006

# --- Spatial Canvas Mobile (spatial-canvas/mobile/.env) ---
EXPO_PUBLIC_API_URL=http://localhost:8000
EXPO_PUBLIC_AUTH_URL=http://localhost:8001

# --- Mind Mapper Mobile (mind-mapper/mobile-bci/.env) ---
EXPO_PUBLIC_API_URL=http://localhost:8002
EXPO_PUBLIC_AUTH_URL=http://localhost:8001

# --- Full-stack docker-compose (docker-compose.full-stack.yml) ---
# SECURITY — generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=your-generated-secret-here
